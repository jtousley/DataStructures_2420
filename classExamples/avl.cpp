
/*
void rotateRight(Node*& t){
	assert(t!=NULL)
	TreeNode<Etype>* kid=t->left=kid->right;
	kid->right=t;
	t=kid;
	return;
}

void rotateLeft(Node*& t){
	assert(t!=NULL)
	TreeNode<Etype>* kid=t->right=kid->left;
	kid->left=t;
	t=kid;
	return;
}

*/